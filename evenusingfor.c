#include <stdio.h>
int main()
{
    // initialize
    // condition check
    // increment/decrement
    int i;
    for (i = 1; i <= 50; i++)
    {
        if (i % 2 == 0)
        {
            printf("\n %d", i);
        }
    }

    return 0;
}