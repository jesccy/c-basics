#include <stdio.h>
int main()
{


int arr[5] = {9,5,3,2,1};







   /*** // int monday = 6, tuesday = 10, wednesday = 17, thursday = 12, friday = 8, saturday = 18, sunday = 24;
    int i, j,a;
    int smallets;
    // printf("%d", monday);
    // array declaration and initiliazation
    int hours_slept[7] = {16, 10, 17, 12, 8, 18, 24};
    int hours_slept_smallest[7];
    hours_slept_smallest[0] = hours_slept[0];
    for (i = 0; i <= 6; i++)
    {

        for (j = i + 1; j <= 6; j++)
        {
            if (hours_slept[i] > hours_slept[j])
            {
               // printf("\n Greater value is ");
                a = hours_slept[i];
                hours_slept[i] = hours_slept[j];
               // printf("\n%d", hours_slept[i]);
                hours_slept[j] = a;
            }
        }
        printf("\n%d", hours_slept[i]);
    }

    for (i = 0; i <= 6; i++)
    {
    }***/

    return 0;
}
